import styled from 'styled-components';
import { StyledInputField } from '../CreateQuiz/formStyles';

export const EditField = styled(StyledInputField)`
	font-size: 3rem;
	border-radius: 5px;
	text-transform: uppercase;
	font-weight: 300;
	margin-left: -0.8rem;
	flex: 0 0 70%;
	width: 100%;
	background-color: transparent;
`;
