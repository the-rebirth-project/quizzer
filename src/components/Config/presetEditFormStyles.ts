import styled from 'styled-components';
import { StyledInputField } from '../CreateQuiz/formStyles';

export const PresetNameField = styled(StyledInputField)`
	font-size: 3rem;
	border-radius: 5px;
	text-transform: uppercase;
	font-weight: 300;
	margin-bottom: -1rem;
	margin-left: -0.8rem;
`;
