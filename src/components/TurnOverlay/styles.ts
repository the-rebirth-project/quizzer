import styled from 'styled-components';
import { CenterText } from '../Overlay/styles';
import { NavBtn } from '../Layout/styles';

export const StyledNavBtn = styled(NavBtn)`
  font-size: 6rem;
`;

export const PlayerTurnText = styled(CenterText)`
  letter-spacing: 3rem;
`;
